import { TableWithFilters } from "@/widgets/table-with-filters"
import type { FC } from "react"
import { config } from "./config"

export const CertSystemsPage: FC = () => <TableWithFilters config={config} />

        
    
